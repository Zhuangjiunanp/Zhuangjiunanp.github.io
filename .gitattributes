# 先排除不想用 LFS 的类型
SOUNDSMUSIC/**/*.m3u8 !filter !diff !merge text
SOUNDSMUSIC/**/*.ts !filter !diff !merge

# 只对特定二进制文件启用 LFS（如果需要）
# SOUNDSMUSIC/**/*.mp4 filter=lfs diff=lfs merge=lfs -text
# SOUNDSMUSIC/**/*.zip filter=lfs diff=lfs merge=lfs -text