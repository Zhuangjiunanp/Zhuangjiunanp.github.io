name: Deploy English Resources

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Create file listing
      run: |
        echo "📦 生成文件列表..."
        # 创建文件列表JSON
        find english -type f -not -path "*/.*" | grep -v ".github" > file_list.txt
        echo "✅ 文件列表生成完成"
        
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: .

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
