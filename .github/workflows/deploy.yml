name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Run npm scripts
      run: |
        echo "📦 运行 npm 脚本..."
        npm run test
        # 如果有其他脚本也可以在这里运行
        # npm run build

    - name: Setup Environment
      run: |
        echo "GEETEST_ID=${{ secrets.GEETEST_ID }}" >> $GITHUB_ENV
        echo "GEETEST_KEY=${{ secrets.GEETEST_KEY }}" >> $GITHUB_ENV

    - name: Test Secrets
      run: |
        echo "✅ Geetest 配置测试成功"
        echo "ID 长度: ${#GEETEST_ID}"
        echo "KEY 长度: ${#GEETEST_KEY}"
        echo "Node.js 版本: $(node --version)"
        echo "NPM 版本: $(npm --version)"

    - name: Create npminstall.html
      run: |
        mkdir -p dist
        # 创建 npminstall.html 而不是 index.html
        node -e "
        const fs = require('fs');
        const html = \`
        <!DOCTYPE html>
        <html>
        <head>
            <title>NPM Install 测试页面</title>
            <style>
                body { font-family: Arial, sans-serif; margin: 40px; }
                .success { color: #28a745; }
                .info { color: #17a2b8; }
                .card { 
                    border: 1px solid #ddd; 
                    padding: 20px; 
                    margin: 10px 0; 
                    border-radius: 8px;
                    background: #f8f9fa;
                }
                .header { 
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white;
                    padding: 30px;
                    border-radius: 10px;
                    text-align: center;
                }
            </style>
        </head>
        <body>
            <div class=\"header\">
                <h1>🚀 NPM + GitHub Actions 测试成功！</h1>
                <p>这个页面通过 GitHub Actions 自动部署</p>
            </div>
            
            <div class=\"card\">
                <h2>📦 NPM 环境信息</h2>
                <p><strong>Node.js 版本:</strong> \${process.version}</p>
                <p><strong>NPM 版本:</strong> \${process.env.npm_version || '未知'}</p>
                <p><strong>运行系统:</strong> \${process.platform}</p>
            </div>
            
            <div class=\"card\">
                <h2>🔑 极验配置状态</h2>
                <p><strong>GEETEST_ID:</strong> <span class=\"success\">✅ 已正确配置</span></p>
                <p><strong>GEETEST_KEY:</strong> <span class=\"success\">✅ 已正确配置</span></p>
                <p><em>Secrets 在 GitHub Actions 中安全存储</em></p>
            </div>
            
            <div class=\"card\">
                <h2>📊 构建详情</h2>
                <p><strong>构建时间:</strong> <span id=\"buildTime\"></span></p>
                <p><strong>提交哈希:</strong> ${{ github.sha }}</p>
                <p><strong>触发分支:</strong> ${{ github.ref }}</p>
            </div>
            
            <div class=\"card\">
                <h2>🔗 其他页面</h2>
                <p><a href=\"/\">查看主页面 (index.html)</a></p>
                <p><a href=\"https://github.com/${{ github.repository }}\">查看源代码</a></p>
            </div>
            
            <script>
                document.getElementById('buildTime').textContent = new Date().toLocaleString();
                // 添加一些交互效果
                document.querySelectorAll('.card').forEach(card => {
                    card.addEventListener('mouseover', function() {
                        this.style.transform = 'translateY(-2px)';
                        this.style.boxShadow = '0 4px 8px rgba(0,0,0,0.1)';
                    });
                    card.addEventListener('mouseout', function() {
                        this.style.transform = 'translateY(0)';
                        this.style.boxShadow = 'none';
                    });
                });
            </script>
        </body>
        </html>
        \`;
        fs.writeFileSync('dist/npminstall.html', html);
        "
        
        echo "📁 创建的文件:"
        ls -la dist/

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
        publish_branch: gh-pages
        force_orphan: false
        commit_message: "Deploy: npminstall.html with NPM $(date +'%Y-%m-%d %H:%M')"

    - name: Show Access URLs
      run: |
        echo "🎯 访问地址:"
        echo "主页面: https://${{ github.repository_owner }}.github.io"
        echo "NPM测试页面: https://${{ github.repository_owner }}.github.io/npminstall.html"
        echo ""
        echo "📝 说明:"
        echo "- 主页面显示你原来的 index.html"
        echo "- NPM测试页面显示这个工作流生成的内容"
