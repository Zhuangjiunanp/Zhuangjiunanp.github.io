name: Deploy without Geetest Validation

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: |
        echo "📦 安装项目依赖..."
        npm install
        echo "✅ 依赖安装完成"

    - name: Build and deploy
      run: |
        echo "🚀 开始构建和部署..."
        # 这里添加您的构建和部署命令
        # 例如: npm run build, npm run deploy 等
        echo "✅ 部署流程完成"
