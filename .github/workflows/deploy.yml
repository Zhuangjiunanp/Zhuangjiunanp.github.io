name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Run npm scripts
      run: |
        echo "📦 运行 npm 脚本..."
        npm run test
        # 如果有其他脚本也可以在这里运行
        # npm run build

    - name: Setup Environment
      run: |
        echo "GEETEST_ID=${{ secrets.GEETEST_ID }}" >> $GITHUB_ENV
        echo "GEETEST_KEY=${{ secrets.GEETEST_KEY }}" >> $GITHUB_ENV

    - name: Test Secrets
      run: |
        echo "✅ Geetest 配置测试成功"
        echo "ID 长度: ${#GEETEST_ID}"
        echo "KEY 长度: ${#GEETEST_KEY}"
        echo "Node.js 版本: $(node --version)"
        echo "NPM 版本: $(npm --version)"

    - name: Create Demo Page with npm
      run: |
        mkdir -p dist
        # 使用 Node.js 生成更动态的页面
        node -e "
        const fs = require('fs');
        const html = \`
        <!DOCTYPE html>
        <html>
        <head>
            <title>Geetest + NPM 配置测试</title>
            <style>
                body { font-family: Arial, sans-serif; margin: 40px; }
                .success { color: #28a745; }
                .info { color: #17a2b8; }
                .card { 
                    border: 1px solid #ddd; 
                    padding: 20px; 
                    margin: 10px 0; 
                    border-radius: 8px;
                }
            </style>
        </head>
        <body>
            <h1 class=\"success\">✅ Geetest + NPM 配置成功！</h1>
            
            <div class=\"card\">
                <h2>📦 NPM 信息</h2>
                <p>Node.js 版本: <strong>\${process.version}</strong></p>
                <p>NPM 版本: <strong>\${process.env.npm_version || '未知'}</strong></p>
            </div>
            
            <div class=\"card\">
                <h2>🔑 极验配置状态</h2>
                <p>GEETEST_ID: <strong class=\"success\">已配置</strong></p>
                <p>GEETEST_KEY: <strong class=\"success\">已配置</strong></p>
            </div>
            
            <div class=\"card\">
                <h2>📊 构建信息</h2>
                <p>构建时间: <span id=\"buildTime\"></span></p>
                <p>用户代理: <span id=\"userAgent\"></span></p>
            </div>
            
            <script>
                document.getElementById('buildTime').textContent = new Date().toLocaleString();
                document.getElementById('userAgent').textContent = navigator.userAgent;
            </script>
        </body>
        </html>
        \`;
        fs.writeFileSync('dist/index.html', html);
        "
        
        echo "📁 创建的文件:"
        ls -la dist/

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
        publish_branch: gh-pages
        force_orphan: false
        commit_message: "Deploy with NPM: $(date +'%Y-%m-%d %H:%M:%S')"
